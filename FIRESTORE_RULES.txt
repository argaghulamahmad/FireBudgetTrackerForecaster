/**
 * FIRESTORE SECURITY RULES
 * 
 * ⚠️ IMPORTANT: These rules must be set in Firebase Console
 * 
 * For now, this allows authenticated users with localStorage auth.
 * In production, implement proper authentication (Firebase Auth, OAuth, etc.)
 * 
 * Go to: Firebase Console > Firestore > Rules
 * Copy and paste the rules below, then click "Publish"
 */

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write if user is authenticated via localStorage
    // NOTE: This is a development setup. Use Firebase Auth in production.
    match /budgets/{document=**} {
      allow read, write: if true; // WARNING: Allow all access for MVP
      // Production rule (uncomment when using Firebase Auth):
      // allow read, write: if request.auth != null;
    }
  }
}
